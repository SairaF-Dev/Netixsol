# Core (no external deps needed to run this offline demo pipeline)
# Everything below is for the PRODUCTION path (swap-in embedder/generator backends)

# Production embeddings + generation
openai>=1.30.0
anthropic>=0.30.0

# Production vector store
chromadb>=0.5.0

# Agent framework (Day 1/3 integration)
langchain>=0.2.0
langgraph>=0.1.0
